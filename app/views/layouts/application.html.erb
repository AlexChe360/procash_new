<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Procash" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag 'tailwind', media: 'all', 'data-turbo-track': 'reload' %>
    <%= stylesheet_link_tag 'application.tailwind', media: 'all', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <%= action_cable_meta_tag %>
  </head>
  <body>
    <main data-controller="bottom-sheet payment" class="bg-gray-100 min-h-screen container w-full p-5">
      <header class="flex items-center justify-between mb-5">
        <div class="flex justefy-center">
          <%= image_tag 'logo.png', alt: 'logo_image', class: 'h-10'%>
        </div>
        <div class="language-switcher font-medium">
          <%= link_to "RU", url_for(locale: :ru, **request.query_parameters), class: ("text-green-600" if I18n.locale == :ru) %> |
          <%= link_to "EN", url_for(locale: :en, **request.query_parameters), class: ("text-green-600" if I18n.locale == :en) %> |
          <%= link_to "KZ", url_for(locale: :kz, **request.query_parameters), class: ("text-green-600" if I18n.locale == :kz) %>
        </div>
      </header>
      <%= yield %>
      <div class="max-w-4xl mx-auto p-6 bg-white shadow-lg rounded-lg mt-10">
        <div class="flex items-center mb-4">
          <div class="flex items-center justify-center w-6 h-6 bg-green-600 text-white rounded-full">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 00-1.414 0L8 12.586 5.707 10.293a1 1 0 10-1.414 1.414l3 3a1 1 0 001.414 0l8-8a1 1 0 000-1.414z" clip-rule="evenodd" />
            </svg>
          </div>
          <p class="ml-4 text-gray-800">
            <a href="https://pinta.kz/policy" target="_blank" class="text-black underline"><%= t("main.privacy_policy")%></a>
          </p>
        </div>
        <div class="flex items-center mb-4">
          <div class="flex items-center justify-center w-6 h-6 bg-green-600 text-white rounded-full">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 00-1.414 0L8 12.586 5.707 10.293a1 1 0 10-1.414 1.414l3 3a1 1 0 001.414 0l8-8a1 1 0 000-1.414z" clip-rule="evenodd" />
            </svg>
          </div>
          <p class="ml-4 text-gray-800">
            <a href="https://pinta.kz/public-offer" target="_blank" class="text-black underline"><%= t("main.public_offer")%></a>
          </p>
        </div>
        <div class="flex items-center">
          <div class="flex items-center justify-center w-6 h-6 bg-green-600 text-white rounded-full">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 00-1.414 0L8 12.586 5.707 10.293a1 1 0 10-1.414 1.414l3 3a1 1 0 001.414 0l8-8a1 1 0 000-1.414z" clip-rule="evenodd" />
            </svg>
          </div>
          <p class="ml-4 text-gray-800">
            <a href="https://pinta.kz/payment-procedure" target="_blank" class="text-black underline"><%= t("main.payment_procedure")%></a>
          </p>
        </div>
      </div>
    </main>
  </body>
</html>
